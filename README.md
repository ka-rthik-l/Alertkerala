
  # AlertKerala

  This is a code bundle for AlertKerala. The original project is available at https://www.figma.com/design/S0rGpOGyuqXgzhKv9DaU6O/AlertKerala.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  